{
  "compilerOptions": {
    "outDir": "./dist/", // 输出目录
    "noImplicitAny": true, // 为 false 时，如果编译器无法根据变量的使用来判断类型时，将用 any 类型代替。为 true 时，进行强类型检查，会报错
    "module": "es6", // 组织代码方式(commonjs, AMD)
    "target": "es5", // 编译目标平台(es3, es5, es2015)
    "jsx": "react",
    "allowJs": true, // 包含js文件
  }
}